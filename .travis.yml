language: node_js
node_js:
  - 11
cache:
  directories:
    - node_modules
stages:
  - lint
  - test
jobs:
  include:
    - stage: lint
      script:
        yarn run lint
    - stage: test
      script:
        yarn run test:unit
